{
  "name": "stylelint-examples",
  "private": true,
  "scripts": {
    "compile": "sass ./styles/main.scss dist/main.css --no-source-map",
    "lint": "stylelint styles/*.scss"
  },
  "devDependencies": {
    "sass": "^1.55.0",
    "stylelint": "^14.14.0",
    "stylelint-scss": "^4.3.0"
  }
}
